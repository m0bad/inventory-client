export enum UserRoles {
  admin = 'Admin',
  customer = 'Customer',
  supplier = 'Supplier',
}

// export const userRoles = {
//   admins: [String(UserRoles.admin)],
//   customers: [String(UserRoles.customer)],
//   suppliers: [String(UserRoles.supplier)],
//   all: [String(UserRoles.admin), String(UserRoles.customer), String(UserRoles.supplier)],
// };
